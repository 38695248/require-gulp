//滚动条到底部
;(function (factory) {
    if (typeof define === "function" && define.amd) {
        // AMD模式
        define([ "jquery" ], factory);
    } else {
        // 全局模式
        factory(jQuery);
    }
}(function ($) {
    $.fn.scrollToEnd = function(opt) {  
		var n = $(this);
		if(opt.type == 'end'){ 
			n.scrollTop(n[0].scrollHeight+50);
		}
	};
}));